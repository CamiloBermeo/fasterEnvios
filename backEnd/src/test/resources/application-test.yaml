spring:

  datasource:
    url: jdbc:h2:mem:faster_envios_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: root
    password: ""
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  flyway:
    enable: false

client:
  openRouteService-apiKey: ${test_key_placeholder:un_api_key_ficticio_para_local}

api:
  security:
    token:
      secret: "un_secreto_super_largo_y_seguro_para_pruebas_locales_123456"
app:
  cors:
    allowed-origins: ${http://localhost:5173:cors-falso-para-test}